#include <stdio.h>
int main() {
   /* int no1 = 85, no2 = 75;
    if(no1 > no2){
        printf("\n max no is %d", no1);   }
    else{
        printf("\n max no is %d", no2);*/
      //  int max = (no1 > no2) ? no1 : no2;
       // printf("\n max no is %d", max);
  /* if ( no1 > no2 * 2 / 10 );
    printf("\n %d", no1 );
    else (no1 > no2 );
        printf("\n %d", no2);*/
        
    
    return 0;
}
